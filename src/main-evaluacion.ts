/*import { NestFactory } from '@nestjs/core';
import { EvaluacionModule } from './modules/evaluacion/evaluacion.module';

async function bootstrap() {
  const app = await NestFactory.create(EvaluacionModule);
  await app.listen(3000);
}
bootstrap();*/